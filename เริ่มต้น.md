# เริ่มตรงนี้

เปิด PowerShell:

```powershell
cd C:\se_g8
$pythonPath = (Get-ItemProperty 'HKCU:\Software\Python\PythonCore\3.11\InstallPath').ExecutablePath
& $pythonPath run_all.py
```

รันครบ A–D ทั้ง 6 สถานการณ์ ซ้ำ 5 รอบ สร้างกราฟ ตาราง และบทที่ 6–7 อัตโนมัติ ใช้เวลาประมาณ 10 นาที

เดโมสั้น: `& $pythonPath run_all.py --demo` (1 รอบ)

ผลอยู่ใน `results\วันเวลา\` เปิด `บทที่6-7.md` เป็นลำดับแรก

ลำดับอ่านโค้ดเพื่อพรีเซนต์: common.py → mock_api.py → strategies.py → experiment.py → analyze.py

รายละเอียดวิธีวัดผลและข้อจำกัดอยู่ใน README.md
